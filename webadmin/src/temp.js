// function unvs(p, e, i) {
//     [p, e, i]
//     return
// }

// class unvs {
//     constructor() {
//         this.e = e
//         this.p = p
//         this.i = i
//     }

// }
function addtiton(a, b) {
    switch (action) {
        case 'plus':
            return `the sum of '${a} plus ${b} equal to ${result}'`;
        case 'added to':
            return  ``
    }
    let result = and(a, b) || plus(a, b) || added_to(a, b)
    return `the sum of '${a} plus ${b} equal to ${result}'`
}; const exp = console.log(addtiton(2, 3))